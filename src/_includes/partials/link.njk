{# <a class="home__cta text-link {{ linkStyle }}" href="{{ linkURL }}"><span data-content="{{ linkText }}" aria-hidden="true"></span>{{ linkText }}</a> #}

<div class="text-link-wrap {{ linkTheme }}">
  <a href="{{ linkURL }}" {{ linkData }} {% if linkClass %}class="{{ linkClass }}"{% endif %}">{{ linkText }}</a>
  <div class="text-link-line"></div>
</div>